export enum UserLanguage {
  EN = 'en',
  IT = 'it'
}

export enum PartnerType {
  CLIENT_SUPPLIER = 'client_supplier',
  CLIENT = 'client',
  SUPPLIER = 'supplier',
  CONTACT = 'contact',
  OTHER = 'other'
}
